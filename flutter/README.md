# Я Flutter shell

Empty native window for a later compile to iOS, Android, and HarmonyOS.

v0 logic lives in `../web`. Point a WebView at those files, or port `app.js` into Dart when you are ready to ship stores.

Do not treat this folder as the running app yet.
